import styled from 'styled-components';

type ServicesPageProps = {};

const StyledWrapper = styled.div`
  display: flex;
`;

export const ServicesPage = ({}: ServicesPageProps) => {
  return <StyledWrapper>ServicesPage</StyledWrapper>;
};
