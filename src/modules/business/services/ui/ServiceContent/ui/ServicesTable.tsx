import styled from 'styled-components';

type ServicesTableProps = {};

const StyledWrapper = styled.div`
  display: flex;
`;

export const ServicesTable = ({}: ServicesTableProps) => {
  return <StyledWrapper>ServicesTable</StyledWrapper>;
};
